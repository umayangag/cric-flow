//go:build integration

package features

import "testing"

// TestParitySkeleton is a placeholder for formula parity tests against known fixtures.
// It is gated under the 'integration' build tag and is skipped until implemented.
func TestParitySkeleton(t *testing.T) {
	t.Skip("Integration parity tests not yet implemented")
}
