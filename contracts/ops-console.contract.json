{
  "$comment": "Generated from go-app/internal/services/pipeline/registry.go. Do not edit by hand; run: go test ./internal/services/pipeline -run TestPipelineContract -update",
  "version": 1,
  "pipeline_steps": [
    {
      "id": "import",
      "command": "cricsheet-import",
      "label": "Import",
      "optional": false,
      "requires": []
    },
    {
      "id": "retrain",
      "command": "xi-retrain",
      "label": "Retrain",
      "optional": false,
      "requires": [
        "import"
      ]
    },
    {
      "id": "evaluate",
      "command": "xi-evaluate",
      "label": "Evaluate",
      "optional": true,
      "requires": [
        "import"
      ]
    },
    {
      "id": "reload",
      "command": "xi-reload",
      "label": "Reload",
      "optional": false,
      "requires": [
        "retrain"
      ]
    }
  ],
  "data_steps": [
    {
      "id": "fetch",
      "command": "dataset-fetch",
      "label": "Fetch Dataset",
      "optional": false,
      "requires": [],
      "prerequisite": "Downloads from the Cricsheet host allowlist into the staging directory. The server needs outbound network access."
    },
    {
      "id": "extract",
      "command": "dataset-extract",
      "label": "Extract Dataset",
      "optional": false,
      "requires": [],
      "prerequisite": "Needs a .zip in the staging directory — fetch one first, or place one there. Extraction replaces the dataset directory; the previous contents are kept under staging."
    }
  ],
  "lanes": [
    "compute",
    "data"
  ],
  "rejected_query_params": [
    "use_unified_model",
    "model=unified",
    "use_latest_model"
  ],
  "rejected_body_params": [
    "weather",
    "simulate",
    "use_reconciled_scorecard",
    "include_both_scorecards"
  ]
}
