# Plan 1.6–1.7 — Consolidate cmd/team-select tests and stabilize integration tests

Parent: 1
Active path: 1 → 1.6 (then 1 → 1.7)
Created: 2025-11-09 16:59

## Scope
- 1.6: Merge split orchestrator tests under `go-app/cmd/team-select/` (`orch_*.go`) into a single `orch_test.go` with table‑driven cases grouped per function. Remove redundant files once merged.
- 1.7: Audit, fix, and stabilize integration tests. Keep them under `go-app/integration/` (or `*_integration_test.go`) and ensure deterministic, offline‑by‑default execution.

## Files to create/modify
- cmd/team-select
  - Create/Update: `orch_test.go` — contains all unit cases previously spread across:
    - `orch_error_test.go`
    - `orch_csv_error_test.go`
    - any other `orch_*.go`
  - Remove: the above `orch_*.go` once folded.
  - Add/keep: `helpers_test.go` (if needed) for shared fakes like `failingConnector`, `capturingSelector`.
- integration
  - Update: `go-app/integration/export_fielding_integration_test.go` for stability if needed.
  - Move any misplaced integration‑like tests from `cmd/*` or `internal/*` into `integration/` with clear naming.

## High‑level changes per file
- `cmd/team-select/orch_test.go`
  - Table‑driven subtests:
    - From DB path success and error cases (DB connect error, selector OK paths).
    - From CSV path success and failure cases (CSV selector error).
    - Option propagation checks (team size, min bowlers, require keeper) — ensure no conditional branches inside subtests; use small helpers.
  - Reuse simple fakes for `Connector` and `Selector` interfaces; keep them in the same file or `helpers_test.go`.
- `integration/export_fielding_integration_test.go`
  - Verify and harden:
    - DB availability: already guarded with `t.Skip` when connect fails; keep.
    - Migrations path correctness relative to `go-app/` root.
    - Fixture path correctness for `tests/fixtures/cricsheet`.
    - Exporter command invocation (`go run ./cmd/export-dataset`): ensure `GO_APP_OUTPUT_DIR` is set; keep timeouts deterministic.
    - Reduce flaky sleeps; rely on explicit timeouts and error checking.

## Tests to add/update
- Merge all existing `orch_*` cases into table‑driven groups in `orch_test.go`.
- Ensure no test contains manual `if` branching for assertions; use `t.Fatalf` via helpers where needed.
- Keep integration test assertions strict yet deterministic (CSV headers order, row counts) and ensure no external network calls.

## Acceptance criteria
- cmd/team-select has exactly one unit test file for the orchestrator: `orch_test.go`, table‑driven, covering previous scenarios. No lingering `orch_*.go` unit test files.
- All integration tests pass locally. `export_fielding_integration_test.go` is resilient (skips if DB not available) and deterministic.
- `go test ./go-app/...` passes on a clean checkout without external services (DB‑backed test is skipped when DB is unavailable).
- Formatting and vet checks are clean.

## Verification commands
- From repo root:
  - `go test ./go-app/...`
  - `gofmt -s -l go-app | grep -v '^$'` (expect no output)
  - `go vet ./go-app/...`
- To run integration only or focus:
  - `go test ./go-app/integration -run TestExportFieldingEndToEnd -v`

## Notes
- Keep PR small and focused: one PR for 1.6 (cmd/team-select consolidation) then a second PR for any 1.7 integration hardening if edits are required.
- Follow Conventional Commits (e.g., `test(team-select): consolidate orchestrator tests into table-driven orch_test.go`).