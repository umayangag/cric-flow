# Plan 1.7–1.10 — Integration hardening, docs, quality gates, and PR workflow (continued)

Parent: 1
Active path: 1 → 1.7 → 1.8 → 1.9 → 1.10
Created: 2025-11-09 17:11

## Scope
Continue executing the approved Plan 1 for the Go app by completing:
- 1.7: Audit and harden integration tests; ensure deterministic, offline-by-default execution with graceful skips when dependencies are absent.
- 1.8: Update documentation to codify testing conventions (single unit test file per production file; table-driven; integration location & execution).
- 1.9: Run quality gates (format, vet, lint) and ensure full test suite remains green.
- 1.10: Finalize feature branch PRs with Conventional Commits, keeping changes small and focused; include Active path in PR descriptions.

## Files to create/modify
- go-app/integration/export_fielding_integration_test.go (harden/clarify skips, timeouts, paths)
- go-app/integration/helpers_test.go (add small utilities if needed: runWithTimeout, temp dirs)
- go-app/README.md (add Testing & Conventions section updates)
- Optional: docs/TESTING.md (centralize guidance; referenced from README)

## High-level changes per file
- integration/export_fielding_integration_test.go
  - Ensure DB connectivity check uses short timeout and clear skip message.
  - Validate fixture paths via repo-relative helpers; avoid brittle cwd assumptions.
  - Keep test deterministic; no external downloads; rely on tiny fixtures in tests/fixtures.
- integration/helpers_test.go (if missing functionality is needed)
  - Provide `runWithTimeout(cmd, d)` and small FS helpers reused by integration tests.
- go-app/README.md
  - Document testing conventions:
    - Unit tests: one `*_test.go` per production file; table-driven; no conditional logic in test bodies (use helpers).
    - Integration tests: placed under `go-app/integration/` or named `*_integration_test.go`; should skip gracefully when dependencies (e.g., Postgres) are unavailable.
  - How to run unit vs integration tests; environment variables that affect tests.
- docs/TESTING.md (optional)
  - Deeper guidance, examples of table-driven tests, and mocks usage (`mockery`).

## Tests to add/update
- Ensure integration tests reference deterministic fixtures under `tests/fixtures/`.
- If any unit tests behave as integration (I/O, network, DB), relocate to `go-app/integration/` with setup/teardown.
- Add/adjust table rows rather than new files.

## Acceptance criteria
- Integration tests run deterministically and skip with a clear message if Postgres is not available; no flakiness.
- Documentation describes the single-file unit test convention, table-driven style, and integration test placement and execution.
- `go test ./go-app/...` passes on a clean checkout without DB; integration tests either pass (with DB up) or skip.
- `gofmt -s`, `go vet`, and (if configured) `golangci-lint` are clean.

## Verification commands
- Run unit + integration (DB likely absent, so integration may skip):
  - `go test ./go-app/...`
  - `go test ./go-app/integration -v`
- Formatting / vet:
  - `gofmt -s -l go-app | grep -v "^$"`
  - `go vet ./go-app/...`
- Lint (if configured):
  - `golangci-lint run`

## Execution phases and breadcrumbs
- 1.7 Integration hardening
  - Parent: 1
  - Steps:
    1. Review `export_fielding_integration_test.go` for timeouts, path joins, and fixture access.
    2. Add/adjust helper functions if needed in `integration/helpers_test.go`.
    3. Confirm deterministic behavior: run with and without DB to check skip behavior.
  - Exit criteria: deterministic run; clear skip when DB is down; no brittle paths.
- 1.8 Documentation
  - Parent: 1.7
  - Steps: Update README and optional docs/TESTING.md to reflect conventions and run instructions.
  - Exit criteria: docs merged; links valid.
- 1.9 Quality gates
  - Parent: 1.8
  - Steps: Run `gofmt -s`, `go vet`, and linter; fix any findings; re-run all tests.
  - Exit criteria: all clean; tests green.
- 1.10 PRs
  - Parent: 1.9
  - Steps: Ensure work is on feature branch `test/consolidate-unit-tests`; open small, focused PR(s) with Conventional Commits and Active path in description.
  - Exit criteria: PRs opened; includes summary of consolidation and integration hardening.

## Notes
- Plan ID remains 1; this is a continued sub-plan for steps 1.7–1.10 to maintain the anti-drift rule.
- Do not introduce new top-level plans; reconcile back to Parent 1 after each sub-plan completes.
