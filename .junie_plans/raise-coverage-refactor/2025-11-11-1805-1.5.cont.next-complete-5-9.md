# Plan ID 1.5.cont.next — Complete Sections 5–9 and Close Master Plan

Active path: 1 → 5..9 → 1.5.cont.next
Parent: 1 (Raise Go Coverage > 80%)

This continuation executes the remaining work to complete the master plan after 4.x and the partial execution of 5–7. It follows `.junie/guidelines.md` exactly: feature branches only, TDD, table‑driven tests with per‑testcase assert helpers, deterministic fixtures, mock externals, and no network in unit tests. Do not modify `src/`.

---

## 0. Conventions and Gates (Parent: 1)
- Branching: never commit to main; small PRs with Conventional Commits.
- Tests: table‑driven; no `if` in test bodies; use small assert helpers.
- Mocks: prefer interfaces; mockery deferred if unstable (documented); use local fakes where needed for TDD.
- Coverage gates: CI informational minimum 50% for now; target ≥80% overall at finalization.

Verification commands (global):
- `cd go-app && make fmt-check && make lint && make test`
- `cd go-app && make coverage && make coverage-func`
- `cd go-app && COV_MIN=50 make coverage-check`

Acceptance:
- CI workflow runs fmt/lint/test/coverage and passes with informational gate (≥50%).

Status: planned

---

## 5 — Logging and Configuration (Parent: 1)
Goal: Finalize logger/config tests and ensure env‑driven behavior is stable.

Scope:
- `internal/logger`: keep `SetupFromEnv()` covered (text/json, levels, invalids, idempotency).
- `internal/config`: ensure `Load()`/validation tests exist and remain green.

Deliverables:
- No behavior change; ensure tests remain green; minor docstring clarifications if needed.

Acceptance:
- `go test -cover ./internal/logger ./internal/config` ≥ 80% each locally.

Verification:
- `cd go-app && go test -cover ./internal/logger ./internal/config`

Branch: `feat/logging-config-finalize`

Status: planned

---

## 6 — Adapters Hardening (Parent: 1)
Goal: Ensure adapters compile against interfaces and have focused tests; stabilize ML client alignment to `mlclient.Service`.

Scope:
- `internal/adapters/httpx/std`: already added and tested — keep green.
- `internal/adapters/clock/system`: already added and tested — keep green.
- `internal/adapters/random/mathrand`: already added and tested — keep green.
- `internal/adapters/mlclient/http`: keep tests (happy path, non‑2xx, reload) and align to `mlclient.Service`.
- Ensure `internal/mlclient` packages (`interfaces.go`, `client.go`, optional `mlclient.go` for PredictWin) do not conflict; keep a single concrete type name (e.g., `Client`).

Acceptance:
- `go test -cover ./internal/adapters/...` passes; no duplicate types.

Verification:
- `cd go-app && go test -cover ./internal/adapters/...`

Branch: `chore/adapters-hardening-final`

Status: planned

---

## 7 — Testing Conventions and Tooling (Parent: 1)
Goal: Ensure Makefile targets and coverage gates; keep mockery config minimal while generator is unstable.

Scope:
- `go-app/Makefile`: verify targets `test`, `coverage`, `coverage-func`, `coverage-html`, `coverage-check`, `mocks` exist and work.
- `.mockery.yaml`: keep minimal interfaces (e.g., `internal/mlclient.Service`, `internal/adapters/random.Random`). Document deferral of broader mocks if tool panics.

Acceptance:
- `make coverage` produces `coverage.out`; `coverage-check` passes with `COV_MIN=50` in CI and locally when run with that env.
- `make mocks` prints install hint and exits non‑zero if missing; if installed, it runs with current minimal config (may be skipped locally if tool issues persist).

Verification:
- `cd go-app && make coverage && make coverage-func && COV_MIN=50 make coverage-check`
- `cd go-app && make mocks || true` (informational if mockery panics)

Branch: `chore/testing-tooling-final`

Status: planned

---

## 8 — CI and Linting (Parent: 1)
Goal: Keep CI green with 50% informational coverage gate and coverage artifact upload.

Scope:
- `.github/workflows/go-app-ci.yml`: confirm steps run `fmt-check`, `lint`, `test`, `coverage`, `coverage-func`, and `coverage-check` with `COV_MIN=50`.
- Upload `coverage.out` artifact.

Acceptance:
- CI success on PR; artifact present.

Verification:
- Open PR; verify workflow success and artifact.

Branch: `ci/coverage-lint-final`

Status: planned

---

## 9 — Documentation (Parent: 1)
Goal: Update docs to reflect structure, commands, coverage, mocks policy, and env.

Scope:
- `go-app/README.md`: update sections: Running tests/coverage, Make targets, mockery notes, env vars (`LOG_FORMAT`, `LOG_LEVEL`, `ML_BASE_URL`, DB vars for migrator examples).
- `.env.example`: add the above keys with safe defaults and comments.

Acceptance:
- Commands in README work; `.env.example` present and sane.

Verification:
- Manual review; copy `.env.example` → `.env` and run `make test` successfully.

Branch: `docs/update-readme-final`

Status: planned

---

## 10 — Final Verification and Close Plan 1 (Parent: 1)
- Run final line:
  - `cd go-app && make fmt-check && make lint && make test && make coverage && go tool cover -func=coverage.out | tail -n 1`
- Confirm each `cmd/*/main.go` ≤ 30 LOC (excluding imports/comments).
- Ensure CI green and coverage gate passes with `COV_MIN=50`.
- Stretch goal: raise coverage to ≥80% before marking master plan complete (do not fail PRs below 80% during this step; use informational gates only).

Status: planned

---

## Progress Markers
- 5 Logging/Config: 
- 6 Adapters Hardening: 
- 7 Testing Tooling: 
- 8 CI & Lint: 
- 9 Docs: 
- 10 Final Verification: 

Legend: * = in progress, ✓ = complete, ! = failed
