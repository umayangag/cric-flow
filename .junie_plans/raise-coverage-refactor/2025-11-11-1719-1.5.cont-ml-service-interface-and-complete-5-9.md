# Continuation Plan — Raise Coverage & Refactor (Service interface + finish Phases 5–9)

Parent: Plan 1
Chat: raise-coverage-refactor
Timestamp: 2025-11-11 17:19

This continuation resumes from the last checkpoint. The ML client abstraction has been finalized as `internal/mlclient.Service`. We will align dependents to this interface, stabilize failing packages, and then complete Phases 5–9 of the master plan with clear acceptance criteria and verification commands.


## Plan Hierarchy
- 1.5.cont — Align to `mlclient.Service` and finish Phases 5–9
  - 1.5.cont.1 — Align team-predictor to `mlclient.Service` and fix weatherworker tests
  - 1.5.cont.2 — Finalize Adapters Hardening (mlclient http impl + tests)
  - 1.5.cont.3 — Testing conventions & tooling gates (mocks, coverage)
  - 1.5.cont.4 — CI and Linting
  - 1.5.cont.5 — Documentation


## Active Path and Branch Discipline
- Feature branches only; never commit to main.
- Conventional Commits for each PR.
- TDD: write failing test, then code.
- Do not modify `src/` (prototype reference).


## 1.5.cont.1 — Stabilize with `mlclient.Service` and fix failing tests
Parent: 1.5.cont
Branch: chore/stabilize-ml-service-integration

Scope:
- Update `internal/services/teampredictor` to depend on `mlclient.Service` (not the removed `Client`/`Predictor` types in callers).
- Adjust `internal/commands/teampredictor` and its tests accordingly.
- Remove invalid nil-checks against non-pointer interfaces; introduce constructor-time validation instead if needed.
- Fix minor test issues noted during coverage run:
  - `internal/commands/weatherworker/runner_test.go`: remove unused variable `r2`.
  - `internal/services/weatherworker/service_test.go`: remove unused import.

Files to touch (expected):
- `go-app/internal/services/teampredictor/service.go`
- `go-app/internal/services/teampredictor/service_test.go`
- `go-app/internal/commands/teampredictor/runner.go`
- `go-app/internal/commands/teampredictor/runner_test.go`
- `go-app/internal/commands/weatherworker/runner_test.go`
- `go-app/internal/services/weatherworker/service_test.go`

Tests (table-driven, no ifs in test bodies):
- Update teampredictor tests to assert the call into `Service.PredictTeam` via a fake `mlclient.Service`.
- Ensure edge cases around invalid options are covered.

Acceptance:
- `go test ./internal/services/teampredictor ./internal/commands/teampredictor` is green.
- Weatherworker tests compile and pass; no unused vars/imports.

Verification commands:
- cd go-app && go test -cover ./internal/services/teampredictor ./internal/commands/teampredictor
- cd go-app && go test -cover ./internal/commands/weatherworker ./internal/services/weatherworker

Status: *


## 1.5.cont.2 — Finalize Adapters Hardening (mlclient HTTP implementation)
Parent: 1.5.cont
Branch: chore/adapters-mlclient-http

Scope:
- Ensure we have a concrete HTTP implementation that satisfies `mlclient.Service`.
- Keep request-building and response-decoding tested via `httptest.Server`.
- Ensure the type naming avoids prior conflicts (e.g., `HTTPClient` implementing `Service`).

Files to add/update (expected):
- `go-app/internal/adapters/mlclient/http/client.go` (concrete `HTTPClient` implementing `Service`)
- `go-app/internal/adapters/mlclient/http/client_test.go`
- Wire-ups where necessary (only in packages that construct the ML client; do not change `src/`).

Tests:
- Happy-path JSON roundtrip for `PredictTeam`.
- Error status handling (non-2xx).
- Reload no-op or success case (depending on API support); if not supported, ensure method returns a defined error (documented and tested).

Acceptance:
- The HTTP adapter compiles and satisfies `mlclient.Service`.
- Tests for the adapter are green with ≥85% coverage for the adapter package.

Verification commands:
- cd go-app && go test -cover ./internal/adapters/mlclient/http

Status: 


## 1.5.cont.3 — Testing conventions & tooling gates (mocks, coverage)
Parent: 1.5.cont
Branch: chore/testing-tooling

Scope:
- Update `.mockery.yaml` to include `internal/mlclient.Service` with output to `internal/mocks`.
- Ensure `make mocks` works deterministically (Mockery v2.36+).
- Ensure coverage commands and thresholds are available and pass with COV_MIN=80.

Files to touch:
- `go-app/.mockery.yaml`
- Commit the generated mocks under `go-app/internal/mocks` (deterministic, reviewable).
- `go-app/Makefile` (if minor tweaks needed).

Acceptance:
- `make mocks` succeeds and produces artifacts.
- `make coverage` then `make coverage-check COV_MIN=80` passes.

Verification commands:
- cd go-app && make mocks
- cd go-app && make coverage && make coverage-func && make coverage-check COV_MIN=80

Status: 


## 1.5.cont.4 — CI and Linting
Parent: 1.5.cont
Branch: ci/coverage-lint

Scope:
- GitHub Actions workflow to run `make fmt-check`, `make lint`, `make test`, `make coverage`, and `make coverage-check COV_MIN=80` on PRs.
- Upload `coverage.out` as an artifact.

Files to add:
- `.github/workflows/go-app-ci.yml`

Acceptance:
- CI green on PRs and shows coverage artifact.

Verification commands:
- Push branch and open PR; observe CI.

Status: 


## 1.5.cont.5 — Documentation
Parent: 1.5.cont
Branch: docs/update-readme

Scope:
- Update `go-app/README.md` to reflect current structure (cli/commands/services split, adapters, logger/config), how to run tests, generate mocks, and coverage gates.
- Provide `.env.example` capturing relevant env vars (e.g., `ML_BASE_URL`, logging: `LOG_FORMAT`, `LOG_LEVEL`).

Files to add/update:
- `go-app/README.md`
- `go-app/.env.example`

Acceptance:
- README explains commands and developer flow clearly; `.env.example` lists all required env vars.

Verification commands:
- Manual review; ensure all make targets in README exist and work: `make mocks`, `make coverage`, `make coverage-check`.

Status: 


## Global Acceptance & Final Verification
- All steps above are ✓.
- `go test ./...` green.
- `make coverage` shows ≥80%; `make coverage-check COV_MIN=80` passes.
- All command entrypoints under `cmd/*` are thin delegators.

Final verification:
- cd go-app && make mocks && make fmt-check && make lint && make test && make coverage && make coverage-func && make coverage-check COV_MIN=80


## Notes & Constraints
- No network calls in unit tests; use `httptest.Server` for HTTP adapter tests.
- Deterministic seeds and fixtures when randomness exists.
- Keep functions small and clear; prefer interfaces and composition.
- Log success/error paths at command boundaries; do not log in pure services.
