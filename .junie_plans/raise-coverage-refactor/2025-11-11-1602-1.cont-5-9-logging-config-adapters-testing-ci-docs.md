# Plan ID 1.cont — Sections 5–9: Logging/Config, Adapters, Testing Tooling, CI, Docs (Continuation)

Active path: 1 → 5..9 → 1.cont
Parent: 1 (Raise Go Coverage > 80%)

This continuation starts immediately after completing 4.x (including `evaluate` and `tools/migrate`). It follows `.junie/guidelines.md` strictly: feature branches only, TDD, table‑driven tests with per‑testcase assert helpers, deterministic fixtures, and no network in unit tests. Do not modify `src/`.

---

## 0. Conventions and Gates (Parent: 1)
- Branching: never commit to main; small PRs with Conventional Commits.
- Tests: table‑driven; no `if` in test bodies; use small assert helpers.
- Mocks: `//go:generate mockery` near interfaces; `make mocks` deterministic.
- Coverage gates: target ≥80% overall by end of this continuation.

Verification commands (global):
- `cd go-app && make mocks && make fmt && make lint && make test && make coverage && make coverage-func`
- `cd go-app && go tool cover -func=coverage.out | tail -n 1`

Status: planned

---

## 5 — Logging and Configuration (Parent: 1)
Goal: Provide a singleton logger facade and a small config package with validation, both covered with table‑driven tests. Add success‑path logging to key commands where trivial.

Scope:
- `internal/logging`:
  - Ensure `Logger` interface exists (already present as `internal/logging/logger_iface.go`).
  - Add singleton setup `SetupFromEnv()` (if not already) with level parsing from `LOG_LEVEL`.
  - Provide `FromContext/With` helpers as thin wrappers if needed.
  - Tests: level parsing (valid/invalid), default behaviors. No external I/O.
- `internal/config`:
  - Introduce minimal `Config` struct and `LoadFromEnv()` helpers (strings/ints/bools) with validation errors.
  - Focus areas: directories defaults, simple validators (non‑empty, ranges), and any flags/env merging helpers used by CLI packages.
  - Tests: table‑driven for validation and defaulting.

Deliverables:
- Code and tests under `go-app/internal/logging` and `go-app/internal/config`.
- Optional: minimal README note.

Acceptance:
- Logging level parsing covered; `SetupFromEnv()` idempotent.
- Config validation covered with table‑driven tests; no network/file I/O.

Verification:
- `cd go-app && go test -cover ./internal/logging ./internal/config`

Branch: `feat/logging-config`

Status: planned

---

## 6 — Adapters Hardening (Parent: 1)
Goal: Wrap stdlib/externals behind interfaces and ensure correctness with focused tests.

Scope:
- `internal/adapters/httpx/std`: thin wrapper around `*http.Client` implementing `HTTPClient`.
- `internal/adapters/fsx/osfs`: wrapper around std `os` for FS operations if not present.
- `internal/adapters/mlclient/http`: HTTP client for ML service implementing `mlclient.Client` (request build only in unit tests; no network).
- `internal/adapters/clock/system`: returns `time.Now()` implementing `Clock`.
- `internal/adapters/random/mathrand`: wraps `math/rand` implementing `Random`.

Tests:
- Use `httptest` for request building and error propagation.
- Pure unit tests for adapters that don’t need servers (clock/random simple checks with seeded RNG).

Acceptance:
- Adapters compile against interfaces; unit tests pass.

Verification:
- `cd go-app && go test -cover ./internal/adapters/...`

Branch: `chore/adapters-hardening`

Status: planned

---

## 7 — Testing Conventions and Tooling (Parent: 1)
Goal: Ensure Makefile targets and coverage gates; restore curated mockery configuration.

Scope:
- Makefile ensures targets: `test`, `coverage`, `coverhtml`, `coverage-func`, `mocks`, `coverage-check` (with `COV_MIN`, default 80).
- Restore `.mockery.yaml` with only interfaces referenced by tests (e.g., `internal/logging.Logger`, repos used by commands/services, `httpx.HTTPClient`, etc.).
- Document `mockery` install in README if not already.

Acceptance:
- `make mocks` generates deterministic artifacts into `internal/**/mocks`.
- `make coverage` produces `coverage.out`; `coverage-check` passes when `COV_MIN=80`.

Verification:
- `cd go-app && make mocks && make coverage && make coverage-func && make coverage-check COV_MIN=80`

Branch: `chore/testing-tooling`

Status: planned

---

## 8 — CI and Linting (Parent: 1)
Goal: Run fmt/lint/test/coverage in CI with an artifact and coverage gate.

Scope:
- Add/update GitHub Actions workflow: trigger on PR, run `make fmt`, `make lint`, `make test`, `make coverage`, and `make coverage-check COV_MIN=80`.
- Upload coverage artifact (`coverage.out`, optional `coverage.html`).

Acceptance:
- CI green on PRs; coverage artifact present.

Verification:
- CI run shows success and artifacts.

Branch: `ci/coverage-lint`

Status: planned

---

## 9 — Documentation (Parent: 1)
Goal: Update docs to reflect new structure, commands, and testing/tooling procedures.

Scope:
- `go-app/README.md`: structure overview, how to run tests and coverage, how to generate mocks, env vars.
- `.env.example`: document required env vars (`LOG_LEVEL`, DB connection vars if used by examples).

Acceptance:
- Docs accurately reflect the layout and workflow.

Verification:
- Manual review; commands in README work locally.

Branch: `docs/update-readme`

Status: planned

---

## Final Verification and Close Plan 1 (Parent: 1)
- Run final line:
  - `cd go-app && make mocks && make fmt && make lint && make test && make coverage && go tool cover -func=coverage.out | tail -n 1`
- Manually confirm each `cmd/*/main.go` ≤ 30 LOC (excluding imports/comments).
- Mark Plan 1 complete when coverage ≥80% and all gates pass.

Status: planned

---

## Progress Markers
- 5 Logging/Config: 
- 6 Adapters Hardening: 
- 7 Testing Tooling: 
- 8 CI & Lint: 
- 9 Docs: 

Legend: * = in progress, ✓ = complete, ! = failed
