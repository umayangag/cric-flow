# Bug backlog

Bugs found while working a planned item — `docs/FOLLOW_UP_PLAN.md`,
`docs/EXTERNAL_DATA_PLAN.md`, `docs/PRODUCT_ROADMAP.md` — that are outside that item's
scope. A non-blocking bug is
recorded here and left alone; a blocking one gets its own `fix/<slug>` PR stacked under the
item, and the row names it.

| id | found during | symptom | where in the code | why it matters | blocking | status |
|---|---|---|---|---|---|---|
| B-1 | A-1 | Reading team-level context for a team, venue or head-to-head pair the state has never seen *writes* that key into the state: `team_context` indexes `defaultdict`s, so a serving request naming an unknown opposition id (or one that names teams but no venue) grows `team_elo`, `team_results`, `head_to_head`, `venue_bat_first` and `team_venue_matches` by one entry per read. The numbers served are the right neutral ones; the loaded state is not the state that was loaded. | `ml-service/ml/xi/ratings.py`, `RatingState.team_context` (and `update`, whose `defaultdict` indexing is correct there) | The D-7 defect class (plan §10.6): a read that mutates the serving state. `_read_slots` closed it for players in P-5; the keyed tables were left as they were. `fixture_context` (A-1) reads with `.get` for this reason. Harmless to the numbers today; it is what makes a long-lived `XiStore` drift under traffic and would make an H-8 comparison after serving depend on what was served. | no | **fixed — the read no longer writes (2026-09-06).** `team_context` reads all five tables with `.get`, defaulting to exactly what the `defaultdict`s would have manufactured (`ELO_INITIAL`, an empty history, `[0.0, 0.0]`, `0`), so a request naming an unknown opposition, an unknown ground or no ground at all leaves the state exactly as the artifact left it. **No number moved, and it was measured, not argued**: the pre-fix `team_context` was loaded from the previous commit beside the new one and the two were run against the same state over seven fixtures — a seen fixture, one and both teams unknown, an unknown venue, an unnamed venue, a pair that has not met in the format, and an unrated format — and every value is identical to the last float. Over those same seven reads the old state grew from `{elo 5, results 5, h2h 8, venue 3, team-venue 5}` to `{11, 11, 12, 6, 12}` — 26 entries written by reading — and the fixed one grew by none. `update` keeps its `defaultdict` indexing, which a second test pins: writing on write is the point there. |
| B-2 | A-5 | `POST /admin/reload` with no `?run=` loaded the run `current` already named instead of the newest one. `current` is set by every reload, so on any box that had reloaded once, the reload after a retrain re-loaded the run already serving: 200, run plan complete, run history green, and `/xi/status` still reporting the older run's `ratings_through`. Two runs built on the dev box were never served — the database held matches through 2026-09-01 while the served ratings ran through 2026-08-25 (9 days, H-11 refuses at 14). | `ml-service/app/main.py`, `admin_reload`; the belief was written down in `go-app/internal/server/step_work.go` (`StepRequest.RunID`: "the retrain before it in the same plan has just written that pointer" — a retrain publishes nothing) | It is the publish step of a three-step pipeline silently not publishing, which no green step, plan or run-history row could show. Fatal to A-5: a cadence that never publishes lets the served ratings age until H-11 refuses predictions, whatever the rhythm. | **yes** | fixed — PR #246 (`fix/reload-publishes-the-new-run`) |
| B-3 | A-5 | A retrain at today's cutoff writes a manifest with `formats: []` and `metrics: {}`. Rows at or after the cutoff are the holdout, and there are none after today, so every format reports `n_holdout: 0` and `manifest_metrics` skips a report entry with no `objective` block. The run is fine — all four formats' models are written and served — but its manifest cannot answer "is this run usable?", and the Workbench's run summary renders blank for every scheduled run. Measured on run `20260903T160602Z-0e1e39c2`: 21,096 rows, four formats trained, `formats: []`. | `ml-service/ml/xi/retrain.py`, `manifest_metrics` (skips `if "objective" not in report`); the cutoff comes from `go-app/internal/services/pipeline.DefaultCutoff` (today UTC), which a run plan cannot override | Not wrong, but weak evidence where the plan says evidence should be (H-16). Training on everything is what a refresh should do, so the fix is not a different cutoff: it is for the manifest to say *why* it has no holdout metrics rather than to omit the formats it trained. | no | **fixed — the manifest tells the two cases apart (2026-09-06).** `headline_metrics` now carries an entry for every format the run *trained*, scored or not: `n_train` and `n_holdout` always, the two AUCs only when there was a holdout to compute them on. `formats` is therefore what the run trained, not what it managed to score, and a new `format_notes` (manifest and `/xi/status` summary) says per format why the numbers are missing — `trained on N rows but not scored: … (0 rows at or after the cutoff)` versus `not trained: insufficient training rows (N rows before the cutoff)`, both quoted from the run's own report rather than re-derived. So an empty `formats` now means, and only means, that nothing was trained. **The cutoff was not touched**: `DefaultCutoff` still returns today UTC and a run trains on exactly what it trained on before — training on everything is what a refresh should do, and changing it would move what every future run sees, which is a pipeline decision and not a manifest fix. The Workbench renders the notes beside the metrics table (§8.7: the reason is where the answer is read, not only in the training log), and the retrain also logs one warning per unscored format. Pinned end to end on a synthetic history: a cutoff after the last match writes `formats: ["T20"]` with `n_holdout: 0` and a note; a cutoff too early to fit anything writes `formats: []` with a *not trained* note; a cutoff that leaves a holdout is unchanged — both AUCs, no note. |
| B-4 | X-1a | Five `internal/db` integration tests failed against a real database with `ERROR: null value in column "match_id" of relation "match" violates not-null constraint`. `insertAppearance` inserted into `match` without a `match_id` (and without `original_match_type`, also `NOT NULL`), and neither column has a default — `match_id` is assigned by the importer from the Cricsheet file, not by a sequence. The tests therefore cannot have passed since they were written. They are gated behind `RUN_DB_TESTS=1`, which `make check-all` does not set, so nothing had ever reported it. | `go-app/internal/db/repo_selection_integration_test.go`, `insertAppearance` (and the four cases through it, plus `TestPlayerStatusStore_PromotionAndDemotionAreOneStateChange_Integration`) | D-12's pool-window arithmetic and its ledger promotion/demotion path are the parts of that item with no working test against the database — the half most likely to be wrong in SQL rather than in Go. A gated test that has never run is a test that does not exist. | no | fixed — PR #255 (`fix/b-4-b-6-integration-tests`). The fixture now gives each match an explicit id and match type, the way the importer does. With the tests running, **the D-12 SQL they cover was found correct**: the window is closed at `since` and half-open at the cutoff, `last_played` is the latest appearance, and a promotion reaches `player.is_retired`, `player_status` and `player_status_event` together. Two of the tests were passing vacuously and were tightened — a `LastPlayed` assertion inside a `continue` loop that skipped an absent row, and a case named for the cutoff boundary that had no appearance on the cutoff day; a fourth fixture player now supplies one. **43 database integration tests run**, and `.github/workflows/go-app-ci.yml` now runs them on every PR (`make -C go-app test-db`) |
| B-5 | X-1a | A `TRUNCATE` in an integration fixture must name *every* table with a foreign key into one it truncates, or Postgres refuses the whole statement. `player_status` and `player_status_event` (D-12) were not added to the identity fixture's list, so all 11 `repo_identity_integration_test.go` cases failed with `cannot truncate a table referenced in a foreign key constraint` — again invisibly, behind `RUN_DB_TESTS=1`. Fixed in this branch, along with adding `player_biography` to both fixtures, since X-1a's own table would have compounded it. | `go-app/internal/db/repo_identity_integration_test.go` and `repo_selection_integration_test.go`, the `TRUNCATE` lists | It is the failure mode a new per-player table causes every time, and the reason it went unnoticed is B-4's reason: the suite is not in `make check-all`. Worth deciding whether it should be. | no | fixed in X-1a's branch. The open question it left — whether the suite belongs in a check that runs — is answered by B-4: it runs in CI now, so a missing table in a `TRUNCATE` list fails a PR instead of nothing |
| B-6 | the licence register (`docs/free-data-prototype-scope`) | The package comment in `go-app/internal/biography/biography.go` said Cricsheet "publishes the register under ODbL". The register page at `https://cricsheet.org/register/` says **ODC-By 1.0** (Open Data Commons Attribution), read 2026-09-04 — attribution only, no share-alike term. The value stored per row (`source_license = CC0-1.0`) is Wikidata's and is correct; only the comment about the join key's source was wrong. | `go-app/internal/biography/biography.go`, the package comment | A licence stated wrongly in code is the kind of claim `docs/config-and-data.md` § Data-source licence register exists to stop: ODbL would oblige share-alike on derived databases, ODC-By does not. Nothing behaves differently today; the record should not disagree with itself. | no | fixed — PR #255. The comment now states ODC-By 1.0, says what it does and does not oblige, and points at the register as the record it must agree with; the register's row no longer reports the discrepancy |
| B-7 | X-3 | The **display** model's swap-violation share has never been measured, and it is **3–7 %**: 0.048 (T20), 0.068 (T20I), 0.031 (ODI), 0.062 (TEST) over the eleven walk-forward folds, on the same one-player upgrade H-4 uses. H-4's 2 % line is checked on the *objective*, whose surface is monotone-constrained on every stem it reads; the display model reads team context as well and its `monotonic_cst` gives those columns 0, so nothing constrains them. Found because X-3's stakes gate wrote the 2 % line into its own triple and then watched the control fail it. | `ml-service/ml/xi/train.py`, `make_display_model` / `contract.monotone_directions`; the probe is `scripts/experiments/xi/x3_match_stakes.py`, `_display_swap_probe` | Nothing selects on the display model — the optimiser reads the objective, which is inside H-4 — so no shipped behaviour is wrong. But the display model is the number a *user* sees change when they swap a player in the Team Lab, and one swap in twenty moves it the wrong way. Either that is acceptable and should be said out loud in the harness, or the constraint set should cover the context columns whose direction is knowable (`team_elo_diff`, `team_form_diff`, `venue_fam_diff`). It is currently neither measured nor stated. | no | **measured and stated** — see § B-7 below. The harness now reports `display_swap_violation_share` per format and `display_swap_monotonicity` per fold (`ml/xi/selection_metrics.display_swap_monotonicity`, one probe shared with `x3_match_stakes.py`), with a glossary entry saying H-4's 2 % line is the objective's contract and not this surface's; the Evaluation tab and the system map carry it. Measured **5.1 % T20, 7.1 % T20I, 3.4 % ODI, 6.1 % TEST** against the database, and no prediction moved (checked against a `main` run on the same database: every measured number bit-identical). **Gate `B-7-display-monotone` is a null and then some:** constraining the three context columns moves the violations the *wrong* way (T20 +0.0073 ± 0.0039, T20I **+0.0638 ± 0.0078**, ODI +0.0171 ± 0.0062, TEST −0.0007 ± 0.0042) at no AUC gain, so nothing shipped. It cannot have worked, and the diagnosis above is corrected: the probe holds team context at the fixture's values, so those columns never move under it. What does move, on **100 %** of upgrades in every format, is `t1_pelo_std` — the only column of `DISPLAY_FEATURE_COLS` an upgrade touches that the contract leaves at 0 — while no constrained column ever moves against its direction. Gate `B-7-pelo-spread` (informs, ships nothing) priced the fix that follows: dropping `t1_pelo_std` / `t2_pelo_std` from the display columns takes the share to **exactly 0.0000 in all four formats** for −0.0004 / +0.0088 / −0.0055 / −0.0047 of AUC. **That trade was taken by decision (PR #267) and B-7 is closed**: the display model no longer reads the Elo spread, `make evaluate` reports `display_swap_violation_share` 0.0000 in every format, and **display AUC was spent to buy it — the gate priced the loss at −0.0055 ODI / −0.0047 TEST and the confirming database run reads −0.0022 ODI / −0.0059 TEST: real losses either way**. See § B-7 part 3 |
| B-8 | P1-2 | The Lab's rating order and the display model disagree about who is better, and Play mode invites a user to act on the first. Swapping an eleven's player for a pool player the rating order ranks *higher* — `select_xi_by_ratings`'s composite of decayed batting and bowling impact plus Elo, which is the order the Lab serves as the eleven in T20 and TEST — **lowers** the displayed probability in 1 of 8 ODI cases, 4 of 8 T20 and 6 of 8 TEST (worst **−0.0805**, TEST), measured through the real stack on the served run. A swap for a player who is at least as good on *every* as-of vector never lowers it (0 falls in 20, all four formats), so B-7's guarantee is intact; what is not true is the stronger thing a user will read into the surface. | `ml-service/ml/xi/optimizer.py` `_greedy_seed`'s ordering against `ml/xi/train.py`'s `DISPLAY_FEATURE_COLS`; the probe is `scripts/probes/p1_2_play_mode.py` (its diagnostic arm) | Play mode's core interaction is "swap a better player in and watch the number move", and the Lab offers no ranking of its own — so the ranking a user brings is either their own judgement or the rating-ordered eleven the Lab itself shows. Neither is the display model's preference. The honest fixes are a *shown* ordering the surface can stand behind (the objective's marginal value already ranks an eleven's players and is on the wire where a format is optimised) or a sentence saying which ordering the guarantee holds under; both are P1-3 / P1-4 work, not P1-2's. | no | **half fixed — P1-3 (2026-09-06); the other half is P1-4.** The "why this player" card's rating-ordered state (T20, TEST) now *says* which ordering it is showing: "picked by rating, not by the win model", the selection rating with its pool percentile, and one sentence — the ordering on screen is the selection's own composite and is not the win model's ranking, so swapping in a higher-rated player can move the displayed probability down. The glossary's `selection_rating` entry carries the same statement and points here. That closes the *shown ordering the surface can stand behind* half for the formats where the surface offers rating order at all, and it changes no model and no selection policy — the measurement above is untouched. What is still open: on a searched format the Lab now has an ordering it can stand behind (the marginal value, on the wire and on the card) but does not yet rank the board by it, and the Play-mode surface itself carries no sentence about which ordering the swap guarantee holds under. Both are P1-4's sweep. **Closed on the surface — P1-4 (2026-09-06).** A searched board is ordered by the marginal value the response already carries and says so ("the objective's own ranking of this eleven, which is what a swap here is measured against"); the rating-ordered board says it is in rating order and not the win model's ranking; the hand-built board says nothing ranks it; and Play mode carries the sentence about which ordering the swap guarantee holds under — a player at least as good on every rated axis, not one the rating order merely ranks higher. The measurement above is untouched: no model and no selection policy changed, and a swap on rating order in T20 and TEST can still lower the number — the surface now says so before it is made. |
| B-9 | P1-3 | go-app's coverage total is **machine-dependent by one statement**, and it sits on the ratchet's rounding boundary. `TestFetch_StopsWhenTheJobContextIsCancelled` cancels the job 50 ms into a transfer the test server dribbles out 4 KB at a time, and asserts only that `Fetch` errors — which it does either way. Whether the error comes from `ctxReader.Read` noticing the cancelled context (covering its `return 0, err`) or from the HTTP transport failing the underlying read first is a race. On this box `ctxReader.Read` is **100.0 %** on five runs out of five; on the CI runner it is **66.7 %**. That one statement moves the go-app total across 76.95, so the same commit measures **77.0 % locally and 76.9 % in CI** — verified by diffing the CI run's own `coverage.out` artifact against a local profile, which differ in exactly this one function and nothing else. | `go-app/internal/services/dataacquire/fetch_test.go`, `TestFetch_StopsWhenTheJobContextIsCancelled`, against `fetch.go`'s `ctxReader.Read` | It cost P1-3 a red check and would cost the next person the same: the ratchet rule says raise the threshold to the measured figure rounded down, and the figure a worker measures locally is not the one the gate applies. It is also a test that does not test what it is named for — it passes whether or not the cancellation reaches `ctxReader`, so the mechanism it exists to protect is unasserted on the machine where it does not fire. The fix is to make the test deterministic (drive `ctxReader.Read` directly with an already-cancelled context, and assert `Fetch` returns `context.Canceled` rather than merely erroring), not to special-case the threshold. | no | **fixed — the mechanism is asserted deterministically.** `ctxReader.Read` is now driven directly by two tests: an already-cancelled context must return `context.Canceled` **and must not read the wrapped reader** (a reader that fails the test if it is read at all), and a live context must pass straight through to it. Neither depends on a race, a sleep or a machine's speed, so the branch is covered on any runner rather than on a fast one. The end-to-end test is kept and renamed for what it actually guarantees — `TestFetch_CancellingTheJobFailsTheTransfer`, now asserting `errors.Is(err, context.Canceled)` rather than merely that something failed; it deliberately does *not* assert which of `ctxReader` and the transport noticed first, because that is the race, and the two tests above are what cover the reader. Stressed 20 runs at `-cpu=1 -race` locally, all green. **The gate moved on the CI run's own figure, not a local one** — which is the mistake this row records: PR #274's Go App CI job read **`Coverage 77.0% meets threshold (>= 76%)`**, up from 76.9 on the same runner before the fix, so `COV_MIN` went 76 → 77 in all three places (`go-app/Makefile`, root `Makefile` `COV_MIN_GO`, `.github/workflows/go-app-ci.yml`) in a follow-up commit on that PR. The workflow comment saying 77 was out of reach is replaced by what actually happened. |
| B-10 | P1-4 | **Must-include is not enforced on the Optimise path.** `go-app/internal/server/ml_xi_client.go` `OptimizeXI` sends `MustInclude: []string{}` whatever the request carried, so a must-include id joins its side's *pool* (P1-1) and the search — or the rating order — may leave him out. Measured on the dev stack (T20I India men, `extra_team1: [11877, 395]`): Ashok Sharma left out, Bumrah in, both silently before P1-4. | `go-app/internal/server/ml_xi_client.go` `OptimizeXI` (the empty `MustInclude`); `go-app/internal/services/predictteam/xi_selection.go` `optimizeSide`; ml-service's `Constraints.must_include` in `ml/xi/optimizer.py` already honours a lock | A user who ticks "must include" reasonably expects the pick to be forced; a label that says "must" over a constraint nothing applies is the D-9 shape with a person in the middle. P1-4 made the label true instead — "added to the pool, checked after" — and put the check on the wire: `selection.must_include` names, per side, how many were asked for and each one the selection left out, so the outcome is on screen in both cases. **The real fix is to pass the resolved registry ids to `/xi/optimize` as `must_include`**, which changes the search: the seed must hold them, every neighbour must keep them, and the marginal values and best alternatives are then conditional on the lock. That is a gated change to the selection (its E5 agreement and the swap-monotonicity check would have to be re-run under the lock), not a product item, and it is not taken here. | no | **fixed — the lock is enforced (2026-09-06).** `OptimizeXI` sends the request's resolved registry ids as `must_include`; go-app resolves them once, on the fixture, so the searched path and Play mode's check read one list. **The default is unchanged and was measured, not argued**: with no must-include the same fixture returns the identical eleven and the identical probability before and after (see the PR). **The gated worry above did not arise**: no measured number moved, because every harness, gate and evaluation call sends an empty `must_include` — the lock only narrows the feasible set of a request that asks for one, and E5 agreement and the swap-monotonicity check never ask. **All three conflicts are on the wire with their reason, never silently resolved (§8.7):** a must-include id this side cannot field (no player row, or no `external_id`) is `400 MUST_INCLUDE_UNRESOLVABLE` from go-app, which is the only side that can name him; more required players than places, a lock that leaves no room for the keeper or the minimum bowlers, and an id the pool does not hold are `422 OPTIMIZATION_CONSTRAINT_ERROR` from ml-service, whose message now names *which* constraint failed instead of "size / bowlers / keeper" (`ml.xi.optimizer.ConstraintConflict`). P1-4's `selection.must_include` is kept and re-read as the postcondition: an ordinary answer holds every id, and a named "left out" player is a lock the stack accepted and did not honour — logged at ERROR and shown as a defect, not as a choice. The Lab's input label is corrected to "required in the eleven", and § 4's must-include omission keeps its place with its reason rewritten: a lock is the caller's input echoed back, not something the selection read about the player |
| B-11 | the day/night calibration experiment (plan §8.13) | **The simulator fits one dispersion to two populations, and its 10-90 interval is wrong in opposite directions on each.** Measured on eleven walk-forward T20 folds, 1,000 draws, and reproduced to three decimals from X-2: first-innings coverage **0.734 by day against 0.841 at night** at nominal 0.80, width 78.4 against 96.3, dispersion ratio 1.099 against 0.865. The pooled 0.771 the harness reports and the Evaluation tab shows is the average of an under- and an over-dispersed half, so the aggregate hides it. The chase reads the same way (0.689 day / 0.759 night), and at night the two innings miss in *opposite* directions under one and the same factor. | `ml-service/ml/xi/simulator.py` `SharedFactor` / `fit_shared_factor` (one residual pool per format); `ml-service/ml/xi/performance.py` `_fit_simulator_calibration`; reported by `ml/xi/sim_harness.py` `_totals_report` | Every interval the product shows for a total — the Lab's simulated range, the scorecard band, the margin — is calibrated for a population the fixture may not belong to: too narrow for a day game, too wide for a night one, by 6 % of nominal coverage in each direction. It is also the common cause behind A-1's "population mix, not venue" null and A-2's "the miss is chase dispersion" null. | no | **open — six candidates gated across three experiments, six recorded nulls; the obstacle is now named and it is not the lever (plans §8.13, §8.14 and §8.15).** *First* (§8.13): conditioning the shared factor on the pre-match day/night label (`SIM-DN-split`) and rescaling it per population (`SIM-DN-scale`) both fix the day side and both overshoot the night side, and both make the night *chase* worse by five standard errors, because the factor is shared by the two innings and at night they want opposite corrections. The lever is wrong, not its setting. *Then* (§8.14, 2026-09-07): the chase gains **a dispersion term the two innings do not share** — the shared factor keeps the pitch, and the chasing side's runs draws carry a second, independent mean-one factor whose log spread is the excess of the chase's fitted residual scale over the draws' own, deconvolved as `fit_shared_factor` deconvolves the first innings and fitted by A-2's censored estimator (`simulator.ChaseDispersion` / `fit_chase_dispersion`, `CHASE_DISPERSION = False`). Gated alone (`SIM-IN-chase`, the isolating arm) and composed with §8.13's per-population factor (`SIM-IN-both`), on T20's eleven folds — a feasibility probe recorded before the arms says ODI (2 of 11) and T20I (4 of 11) can decide neither. **The design's claim holds and it still ships nothing.** The chase's tails go from 0.191 below the 10th percentile against 0.096 above it to **0.084 / 0.097**, both at nominal, in one step and with no level fitted — what A-2 could not do; the day chase 0.689 → 0.811, the first innings 0.734 → 0.760 by day and 0.841 → 0.748 at night, the pooled first-innings width *falling* 84.7 → 81.6, and the night-chase clause that failed at **+0.0829 / +0.0873 ± 0.015** in §8.13 now misses by **+0.0121 ± 0.0205** — half a standard error. Seven of the eight cells move toward nominal. It fails on `night_first_coverage` (−0.0118 ± 0.0189, the right direction at 0.6 of a standard error: 139 night matches a fold is too thin), on both chase-dispersion clauses (`day` at −0.0560 ± 0.0561, 0.998 of a standard error), and on **`e2_not_degraded`** (+0.0043 ± 0.0013, a growth of three standard errors, pooled +0.0062 and still inside the 0.01 tolerance). **The obstacle is now named and it is not the lever**: an *independent* chase term widens the difference between the innings, which is what decides the match, so P(win) moves toward 0.5 — interval calibration bought with probability calibration, a cost the shared factor does not have because a common factor largely cancels in the margin. The term's magnitude is also over-estimated, for two reasons in the estimator rather than the folds: the pitch is removed through the shared factor's *shrunk* per-match value, so what the shrinkage leaves is charged to the chase, and that factor is fitted on the *first* innings. *Then* (§8.15, 2026-09-08): that sharper next step, gated. The same widening drawn so that it **moves with the first innings' realised log residual** in the same draw and fitted against the chase's **own** expectation (`simulator.CorrelatedChaseDispersion` / `fit_correlated_chase_dispersion`), alone (`SIM-IN-corr`) and composed with §8.13's factor (`SIM-IN-corrboth`), with §8.14's arm **re-run as a reference** so the two ways of widening the chase are paired draw for draw against one control — which reproduced §8.14's whole table, cell for cell, on a second run. **E2 moved further the wrong way: +0.0056 ± 0.0013 against §8.14's +0.0039 ± 0.0013 on the same eleven folds**, so the clause the design existed to pass is the one it fails hardest. The reason is one fitted column: the censored regression of the chase's residual on the first innings', each against its own expectation, is **negative in 11 of 11 T20 folds and 10 of 10 ODI folds** (−0.662 to −0.038 in T20) while the control's own slope is **positive in every one** (+0.287 to +0.631). That negative number is **A-2's response to the target**: a regression on the first innings' residual cannot separate the pitch (positive, and already carried by the shared factor) from the chase's reaction to the target it sets (negative, gated and nulled by A-2), so it reads their sum and the term re-introduces A-2's response wearing a dispersion term's clothes — the arm reproduces A-2's recorded tail flip exactly, 0.191 / 0.096 → **0.100 / 0.179**, where §8.14's independent term had reached 0.084 / 0.097. The sign was checked three ways (`--slope-check`): both uncensored readings are biased upward for arithmetic reasons (a chase is lost below a threshold that rises with the first innings; a won chase held at its target sits on a line of slope one), the censored estimator recovers a planted truth in a unit test, and A-2's independent censored fit agrees in every fold. §8.14's *other* diagnosis is confirmed: fitted against the chase's own expectation the leftover independent spread is smaller in **every** fold (0.242–0.428 against 0.306–0.528). **What is left.** A fifth candidate that reads the first innings' residual in *any* parameterisation reads the same sum and gets the same sign, so the two directions are (1) correlate with an estimate of the **pitch** that is not the first innings' own residual — X-2's venue and weather context is the only one the repo has and was never asked this — or (2) fit the **margin** itself, whose coverage (0.50–0.69 at nominal 0.80) is the weakest number the harness reports and the one E2 actually depends on. Nothing shipped in any of the six; `simulator.CHASE_DISPERSION` is `"none"` and both fits stay in the code, off, so the question can be re-asked from the same calibration draws |
| B-12 | the day/night calibration experiment (plan §8.13) | **A fold whose calibration window is too thin to fit a shared factor is pooled into the walk-forward simulator totals with nothing saying so.** `fit_performance` logs a warning and ships that fold's simulator without a shared factor *or* a chase response, and `evaluate.py` then averages its intervals in with every other fold's. Measured: the ODI 2026-03 fold holds 24 complete first innings against the 30 the deconvolution needs, and reads first-innings coverage **0.500**, width 105.2, dispersion **1.48** — the un-widened simulator of plan §8.3. It is what pulls X-2's reported ODI day coverage from 0.739 (the nine folds that have a factor) to 0.715, and its dispersion from 1.048 to 1.092. | `ml-service/ml/xi/performance.py` `_fit_simulator_calibration` (returns `None, None` with a warning); `ml-service/ml/xi/evaluate.py` `_summarize_folds` and `sim_harness.summarize_folds`, which pool every fold's `simulation` node regardless | A published coverage figure silently mixes two different simulators. The per-fold report already carries the answer — `simulation.calibration.shared_factor` is `null` for such a fold — so the information exists and only the summary hides it. Any comparison across releases or arms that includes a thin fold is comparing populations, not models, and the thin folds move with the data as the window rotates (A-4). | no | **fixed — the summary reports both populations (2026-09-07).** `sim_harness.summarize_folds` takes the folds keyed by their window and adds `simulation.shared_factor_folds` to the walk-forward summary: how many folds were scored, how many had a shared factor, **which windows did not**, and the same `totals` node over the calibrated folds alone. Nothing is dropped — excluding a thin fold quietly would be this defect with its sign flipped — so the pooled figure keeps its meaning and the calibrated-only one sits beside it with both denominators visible (§8.7: a substitution is *visible*, not forbidden). Refusing to ship a factorless simulator was considered and rejected here: `_fit_simulator_calibration` is `fit_performance`'s, so raising there would change what a **retrain** produces and what `/simulate` serves — a gated pipeline change, not a reporting fix. **Measured on a full `make evaluate` against the database (2026-09-07, 22,818 matches), pooled → calibrated-only:** *ODI, 10 of 11 folds have a factor (2026-03-01 does not, 24 complete first innings against the 30 needed)* — first innings coverage **0.756 → 0.774**, width 149.6 → 153.8, dispersion **1.028 → 0.984**; chase coverage 0.699 → 0.712, width 130.2 → 133.7, dispersion 1.139 → 1.111. That fold alone reads 0.577 / 106.6 / 1.463 first innings and 0.571 / 94.9 / 1.417 chasing. *T20I, 9 of 10 (2025-06-01 does not, **14** complete first innings — a second thin fold this row had not named)* — first innings coverage **0.783 → 0.792**, width 80.9 → 82.7, dispersion 1.018 → 1.003; chase 0.688 → 0.692, width 66.3 → 68.1, dispersion 1.175 → 1.168; that fold alone 0.698 / 64.7 / 1.156 and 0.651 / 51.0 / 1.241. *T20: all 11 folds have a factor, so the two figures are identical.* *TEST is not simulated.* The Evaluation tab carries it — a warning chip counting the factorless folds, a `folds with a shared match factor` row beside each pooled total, and a caption naming the windows — with an L-1 glossary entry (`shared_factor_folds`) behind the chip; the same chip fires for a **locked window** whose own calibration fitted no factor, which is the other half of the hole (in this run no format's locked window is simulated at all, which is why the tab was showing the pooled fold means). **Nothing trained or served changed, and it was checked rather than argued**: the pooled node the new summary returns is equal field-for-field to what the old code produced from the same folds (asserted on the real report), objective AUC reads 0.6973 / 0.7561 / 0.6725 / 0.6259 — the recorded figures to the last digit — display AUC 0.7296 / 0.7540 / 0.7045 / 0.6397 is #267's expected post-drop reading, H-8 serving parity max abs difference **0.0**, and the gate registry and L-1 completeness gate both pass |
| B-13 | P2-2 | **A refused `POST /admin/reload?run=<id>` unloads the run that was serving.** `XiRegistry.reload` sets `self._store = None` under its lock *before* it attempts `XiStore.load`, so when the named run is refused (`RunArtifactsInvalid`) the registry answers 409 with the reason and is then serving nothing: `/xi/status` reads `loaded: false, run_id: null`, and every live prediction is refused until another reload. Reproduced on the dev stack (2026-09-07): with `20260907T062657Z-6b16045e` serving, a reload naming a copy of it whose manifest lacked `ratings_through` answered **409 `RUN_ARTIFACTS_INVALID`** correctly and left `run_id: null` behind it; `make reload` (no run named) restored the good run. `current_run.json` is untouched, because `set_current` runs after the load, so a restart would also recover. | `ml-service/app/xi_service.py` `XiRegistry.reload` (the `self._store, self._report, self._error = None, None, None` before `XiStore.load`); the comment in `ml-service/app/main.py` `admin_reload` says "the registry keeps serving whatever it had", which is what the code does not do | The rollback path (`?run=<id>`) is exactly where an operator names a run that may be unloadable, and the answer to naming one should be a refusal, not an outage. It is also the mechanism behind the box being found serving nothing after an artifact-policy change: any reload that trips a new load-time check drops the serving run. The fix is to build the new store first and swap under the lock only on success, keeping the refusal in `_error` beside the run still serving; a status that then reads `loaded: true` *and* `error: <the refusal>` needs a sentence saying which run each describes (§8.7). Not blocking P2-2, which only had to reload the good run again. | no | **fixed — the new run is built first and swapped in only once it loaded (2026-09-07).** `XiRegistry.reload` no longer touches what is serving before `XiStore.load`: the store, its report and its directory are built into one `ServedRun` and assigned in one reference swap after the load and `set_current` both succeed, so a refusal -- `RunArtifactsInvalid`, or anything else the load raises -- leaves the previous run serving with the same `run_id` and `ratings_through`, publishes nothing, and keeps the refusal in `error` beside it. **D-6 is not weakened**: the refused run is refused with the same message and the same **409 `RUN_ARTIFACTS_INVALID`**; the hint now names the run still serving. **The lock**: held for the whole reload, load included -- the live path (`store`, `status`, `freshness`) never takes it, each reads the one `_served` reference once, so a slow load blocks no prediction and a reader arriving mid-reload sees the old run or the new one, never `None` and never a store without its report; what the lock holds off is a second reload (whose swap and `set_current` could otherwise interleave with this one's) and the as-of pass (which must not be built against a store about to be replaced). The one case that still unloads is a root with no run at all, which is not a refusal but the state of the disk, and what a restart would serve. **§8.7**: `/xi/status` can now read `loaded: true` *and* `error: <refusal>`; the model docstring and `frontend/src/types.ts` say the two fields describe two runs, the Workbench's run card renders *The last reload was refused and `<run_id>` is still serving: …*, and the Health tab labels the line *Last reload refused*. The comment in `main.py` that claimed the old behaviour is replaced by one that describes the new. **The same shape elsewhere**: `AsOfServer.state_as_of` and `XiRegistry.store_as_of` both construct before they assign, so neither has the defect; `runs.set_current` writes the pointer in place rather than via a temporary file, which is a non-atomic write, not a clear-before-load, and is left alone. Pinned by `test_a_refused_reload_leaves_the_run_that_was_serving_unchanged`, `test_a_load_that_raises_anything_else_also_leaves_the_served_run`, `test_the_next_successful_reload_clears_the_refusal`, `test_a_reader_arriving_mid_reload_is_answered_by_the_run_that_was_serving` (holds the load open and reads through it) and, on the wire, `test_a_refused_reload_still_answers_409_and_the_served_run_stays_on_the_wire` in `ml-service/tests/test_runs_and_reload.py`; the card's sentence by `frontend/src/components/WorkbenchRunSection.test.tsx`. Verified against the dev stack's own artifacts, in a local instance of this branch (the shared container was not rebuilt): serving a copy of `20260907T062657Z-6b16045e`, a reload naming a second copy whose manifest lacked `ratings_through` answered 409 `RUN_ARTIFACTS_INVALID` with the hint *20260907T062657Z-6b16045e is still serving*, and `/xi/status` then read `loaded: true, run_id: 20260907T062657Z-6b16045e, ratings_through: 2026-09-02` with the refusal in `error`; `/artifacts/status` read `current_run` = `loaded_run` = the good run. |
| B-14 | B-11's §8.15 arms (found while #286 was writing up the §8.14 shape change) | **The performance artifact is the one artifact D-6 does not shape-check.** `XiStore.load` joblib-loads `performance_<format>.joblib` and asks nothing of what comes back, so a pickle written before `SimulatorCalibration` changed shape restores with the missing fields at their class defaults instead of being refused. It has now happened twice in two PRs: #285 added `chase_dispersion` and `chase_sample`, and §8.15 turned `chase_dispersion` into either of two classes behind a protocol and `CHASE_DISPERSION` / `FitSpec.chase_dispersion` from a bool into an arm name. Nothing served is wrong *yet*, only because the default arm is `"none"` and `None` is what both an old pickle and a fresh retrain produce. | `ml-service/app/xi_store.py` `XiStore.load` (the performance branch); `ml-service/ml/xi/simulator.py` `SimulatorCalibration`; the rating payload and the win artifact both have the check the performance one lacks | D-6's whole point is that a run whose artifacts predate a contract change is **refused**, not silently reinterpreted: a manifest without `ratings_through` (P2-2) and a win model fitted on the old `DISPLAY_FEATURE_COLS` (B-7) are both refused. The performance artifact is the one where the same class of change is absorbed, and the first arm that ships from `SimulatorCalibration` is the first one where the absorption changes a served interval rather than leaving it alone. | no | **open — recorded, not built.** #286 named the gap and suggested a load-time check as a separate code item; §8.15 widened it a second time and is recording it here rather than fixing it inside a gated experiment, because a check that refuses a run is a serving-path change and belongs in its own PR with its own reproduction. The shape of the fix: `XiStore.load` asks the restored `SimulatorCalibration` for the fields the current class declares and raises `RunArtifactsInvalid` naming the run and the missing field, the way the manifest and the win model already do. |
| B-15 | EVAL-04 (PR #303) | Frontend CI's vitest job failed on runner speed alone: `SystemMapTab › draws every node in the contract and opens details for each one` rendered the tab and clicked through every node of the system-map contract inside vitest's default 5,000 ms per-test timeout, and on GitHub's runner it took 4,987 ms on main's last green run (`fd9e2024`, 2026-09-13) and 5,022–5,036 ms on three consecutive runs of #303 — a branch whose only frontend change is an optional field on a type, erased at runtime, with no frontend file changed on main in between. Two more heavy render tests (`TeamLabTab › honesty surfaces › optimised …`, `… play mode after a swap …`) crossed 5,000 ms on one of the three runs. Locally the same suite passed with the coverage gate (61 files, 511 tests). | `frontend/src/components/SystemMapTab.test.tsx:80`, `frontend/src/components/TeamLabTab.test.tsx`; vitest's default `testTimeout` (nothing in `frontend/vite.config.ts` sets one) | A test that sits 13 ms under its own timeout on a green run is a coin flip on a loaded runner, and it turns a type-only PR red: CI's verdict stops meaning "the change is wrong". | yes — blocked #303 going green; fixed in its own `fix/` PR, not inside an audit finding | **fixed — the walk split into one case per node (2026-09-13, PR #304).** `SystemMapTab.test.tsx`'s single test replaced by `it.each(systemMap.nodes)`, one render-and-click per node with the same three assertions (a heading, the node's summary, "Documented in"), so no case carries the whole 24-node walk and each gets its own 5,000 ms budget instead of one case needing to survive 24 renders and clicks end to end. **Measured, not assumed**: the single test ran 816 ms locally; the 24 per-node cases it replaced ran 42–94 ms each (worst case `harness` at 94 ms) — at the same runner-to-local ratio the CI runs showed (816 ms → 9,651 ms observed on #303, ≈11x), the worst node comes in around 1.0 s against the 5 s budget, an order of magnitude of headroom rather than a few hundred milliseconds. `TeamLabTab.test.tsx`'s two heavy render tests were left alone — out of scope for this PR and not yet confirmed as the same defect shape; still open if they redden a future run. Confirmed not a real render-cost defect: 816 ms for one render plus 24 clicks is unremarkable, and the observed CI/local ratio matches ordinary shared-runner overhead, not a slow component. No global timeout raised, no coverage threshold moved, nothing skipped. |
| B-16 | IMPORT-06 (PR #305) | The batter's wicket-rate ledger charges the striker with every dismissal on the ball: `_update_impact` accumulates `exp_wk - d.wicket` against `batters = d.batter`, so a run out at the non-striker's end — 11,255 of the archive's 353,571 wicket records, 3.2 % — counts against the batter who was facing, and the batter who was actually out is charged nothing. `debut_bat` reads the same term. `bat_wrate` ("dismissals below expectation per ball faced") is therefore the striker's exposure to any wicket on his strike, not a record of his own dismissals, and a batter partnered by a poor runner reads as easier to get out than he is. | `ml-service/ml/xi/ratings.py`, `RatingState._update_impact` (the `exp_wk - d.wicket` term for `bat_wae` and `debut_bat`) | A feature input, so a fix is retrain-flagged and measured, not assumed. `Deliveries.players_out` (IMPORT-06) now names who was out on each ball, so the ledger can charge the dismissed player rather than the striker; nothing in the rating state did before. Found while surveying the archive for IMPORT-06, whose scope is what a wicket *is* and who is credited with taking it, not who is charged with losing it — "no other behaviour change" kept it out of that PR. | no | fixed — PR #349 (FEAT-07, `fix/feat-07`). The ledger reads `players_out`: the striker is charged his own dismissals against the over's expectation on the ball he faced, and a batter out at the other end is charged his on no ball of his own. On the current archive 11,114 of 353,063 dismissals (3.15 %) had been charged to the striker: 11,037 run outs at the non-striker's end, 67 retired out, 7 obstructing the field, 1 handled the ball, 2 timed out. Retrain-flagged; batch 4 measures it |
| B-17 | FEAT-03 (PR #307) | `ml/xi/parity.py`'s `compare` docstring promises a guarantee the code does not give: "Every comparable count is checked rather than a chosen few, so a count added to `DataQuality` later is compared without anyone remembering to come back here." The comparison iterates `_COMPARED_COUNTS`, an explicit tuple, so a count added to the dataclass is compared only if someone remembers to add it there — the opposite of what the docstring says. Five findings in a row (FEAT-04, FEAT-08, IMPORT-05, IMPORT-06, FEAT-03) each had to add their count to the tuple by hand. | `ml-service/ml/xi/parity.py`, `compare` and `_COMPARED_COUNTS` | The same species as the comment B-13 corrected and the `is_retired` filter D-12 found: a docstring that says a check is automatic when it is a list is how the next count goes uncompared. Fix shape, either way: make the comparison automatic over `dataclasses.fields(DataQuality)` minus an explicit exclusion set (`source`, `out_of_scope_matches`, and any other store-side count), so a new field is compared by default and leaving one out is the deliberate act; or correct the docstring to say the tuple is the list and a new count must be added to it. Not fixed in PR #307 -- "no other behaviour change". | no | open |
| B-18 | FEAT-02 (PR #308) | `db.ReadLastFieldedEleven`, the auction projection's seed for an opposing eleven (P3-2), selects a side's most recent match with `HAVING COUNT(*) = auction.TeamSize` over `match_player`, so it **skips every match in which the side fielded twelve** — the 1,362 sides FEAT-02's flag now identifies (a concussion substitute, an impact player, a supersub) — and silently offers an older match instead, or `ErrNoFieldedEleven` when every recent one used a replacement. In a competition that names an impact player every match (the IPL since 2023, the SMAT, the LPL, ILT20) that is most of the side's recent matches, so "the last eleven this side fielded" is in fact the last one it fielded *without* a substitute, dated accordingly. | `go-app/internal/db/repo_last_eleven.go`, `ReadLastFieldedEleven` and `readMatchSidePlayers` | The read is offered to the operator as a dated fact, and the date is wrong in exactly the leagues an auction module is for. Fix shape: read `match_player.is_replacement` (migration `0020`) and take the eleven that started — `WHERE NOT mp.is_replacement` in both queries, counting to eleven over the unflagged rows — the way `ml/xi/sources.py`'s `_PLAYERS_SQL` now does; the twelve-man match then qualifies and its eleven is the one that started. Needs the re-import that sets the flag, since every row reads `false` until then. Not fixed in PR #308 -- "no other behaviour change", and the auction surface is not that finding's. | no | open |
| B-19 | FEAT-02 (PR #308) | **Twenty-four sides in the archive stay over eleven after FEAT-02, and `oversized_squads` counts them — 24 on the archive path, 1,365 on the database until it is re-imported.** Twenty-three carry no `replacements.match` entry at all: 21 Syed Mushtaq Ali Trophy 2022 sides of twelve, 11–30 October 2022, in thirteen files — 1332927, 1332934, 1332950, 1332952, 1332968 (both sides), 1332986 (both), 1332987, 1333005 (both), 1333006 (both), 1333022 (both), 1333024 (both), 1333033 (both), 1333047 (both) — the tournament's impact-player rule, which Cricsheet listed without recording who came in and 2 Women's T20 Challenge 2018 sides of thirteen (1146789, both sides). The twenty-fourth is 1537342 (Lanka Premier League 2026), whose entry names as the man who came in for Dambulla Sixers a player Colombo Kaps lists (`RMMP Rathnayake`, registry `afe830a2`, where Dambulla lists `P Rathnayake`, `c1fdfb46`); the file does not say which Dambulla player joined late, so both languages log it and keep both sides as listed. None of these can be fixed by reading the file: the information is not in it. | the data, not the code — `docs/config-and-data.md` § What the squad record holds records the rule; `ml/xi/quality.py` `oversized_squads` carries the numbers | Recorded so that a reader of the data-quality report can see why `oversized_squads` is 24 rather than zero without re-deriving it, and so that a future Cricsheet release that adds the entries (or corrects 1537342's) is recognised as the thing that moves the count. Fix shape, if one is wanted: a reviewed per-match override under `configs/` naming the replacement for these 24 sides from the scorecards, read by both `cricsheet.Match.ReplacementPlayers` and `sources.replacement_keys` — the shared-rule pattern — or leave them as honest twelves, which is what PR #308 does. Not a code defect; not blocking. | no | open — a data fact, not a bug in the code |
| B-23 | IMPORT-13 (PR #352) | **A full import from an empty database loses seven files, and `make import` stops at the first of them.** `GetOrCreateVenue` upserts with `ON CONFLICT (normalized_name)`, but `venue` still carries the baseline's `venue_venue_name_key UNIQUE (venue_name)` beside `ux_venue_normalized_name`. `ON CONFLICT` only handles its *arbiter* index; a concurrent speculative insert of the same spelling raises `duplicate key value violates unique constraint "venue_venue_name_key" (SQLSTATE 23505)` on the other one. It is a race, not a sequential bug: the same two upserts run one after the other succeed. The cache key is `normalized_name` + city, so the second lookup that reaches the database at all is the one for a ground two files spell identically but place in different cities — **66 of the 892 grounds in the archive**. | `go-app/internal/db/repo_lookup.go` `GetOrCreateVenue`; `go-app/migrations/0001_baseline.sql:1039` and `:1252`; introduced when IMPORT-08 (PR #334, migration `0021_venue_identity.sql`) moved the conflict target to `normalized_name` and left `venue_venue_name_key` in place | Measured: a full 22,905-file import into a scratch database skipped 7 files (`1094702`, `1269073`, `1331386`, `1393877`, `1446104`, `1548659`, `584924`), all with this error. `-fail-fast` defaults to **true**, so the same run under `make import` aborted at file ~4,025 instead. Batch 4 owes a re-import (`match_end_date` is NULL on all 22,905 rows), and that re-import will not complete until this is fixed — most simply by dropping `venue_venue_name_key`, which `normalized_name` has superseded. | **yes — blocks batch 4's re-import** | open — found while measuring IMPORT-13's exposure; reported, not fixed, because it is a different finding |

---

## B-7 — the display surface: measured, and what the gate found

Two separable pieces, in this order, because the first is worth having whatever the second
says.

### Part 1 — measure it and say so (no prediction changed)

`ml/xi/selection_metrics.display_swap_monotonicity` runs H-4's probe against the display
model: one player's five ratings raised by a population standard deviation each, the other
ten and the opponent held, **the match's team context held exactly as the fixture had it**
because a selector cannot change it, and the probability marginalised over the batting
order the way the serving path marginalises it. `make evaluate` reports it per fold
(`display_swap_monotonicity`) and as a walk-forward mean per format
(`display_swap_violation_share`); the glossary explains it and states in its band that
H-4's 2 % line is a contract on the *objective*, whose every column is
monotone-constrained, and not on this surface. The Evaluation tab shows it beside H-4's
tile, without a gate triple, because nothing decides on it. X-3's script no longer keeps
its own copy of the probe — there is one implementation, and the harness and the
experiment share it.

**What it reads.** Against the database: **5.1 % T20, 7.1 % T20I, 3.4 % ODI, 6.1 % TEST**
over the folds, beside H-4's own 0.2 / 0.8 / 0.0 / 0.5 % on the objective. Against the
archive frame the same probe reads 4.8 / 6.8 / 3.1 / 6.2 — the ground-keying difference
between the two sources that X-3 recorded, and the reason the gate below quotes the
archive figures as its control.

**It moves no number, and that is checked rather than argued.** `make evaluate` was run
twice against the same database — once from this branch and once from `main` at
`e529449c` — and the two reports were compared field by field at **exact equality**. Of
126 differences, 124 are `performance.fit.fit_seconds` and
`simulation.latency.ms_per_fixture_*` (wall-clock, and declared non-metrics in the
glossary for that reason) and 2 are the gate-registry entries part 2 adds. **Every
measured number is bit-identical**: objective AUC 0.6973 / 0.7561 / 0.6725 / 0.6259,
display AUC 0.7299 / 0.7533 / 0.7067 / 0.6456, H-4's own swap share 0.0023 / 0.0075 /
0.0000 / 0.0052, every performance target, every simulation figure, specific-vs-typical
and E5, with H-8 serving parity max abs difference **0.0** on both runs. Nothing is fitted
differently; a measurement was added after the models were fitted. (The comparison had to
be against a fresh `main` run: the last recorded run predates X-3's re-import, and the
extra `stage`/`group` columns move the performance model's early-stopping iteration counts
even though nothing about the harness changed.)

### Part 2 — gate `B-7-display-monotone`: a recorded null, and a corrected diagnosis

Eleven quarterly folds, three display seeds, one frame both arms read, everything but the
constraint held (the triple is in `ml/xi/gates.py` and is printed before the run;
`scripts/experiments/xi/b7_display_monotonicity.py`). The control reproduces X-3's recorded
figures exactly, which is the wiring check.

| format | swap (control) | swap (constrained) | Δ ± se | material floor | display AUC (control) | Δ AUC ± se | verdict |
|---|---:|---:|---|---:|---:|---|---|
| T20 | 0.0478 | 0.0551 | +0.0073 ± 0.0039 | −0.0069 | 0.7299 | −0.0003 ± 0.0008 | no |
| T20I | 0.0684 | 0.1322 | **+0.0638 ± 0.0078** | −0.0121 | 0.7495 | −0.0006 ± 0.0063 | no |
| ODI | 0.0307 | 0.0478 | +0.0171 ± 0.0062 | −0.0027 | 0.7083 | +0.0009 ± 0.0017 | no |
| TEST | 0.0616 | 0.0609 | −0.0007 ± 0.0042 | −0.0104 | 0.6431 | −0.0018 ± 0.0051 | no |

Not a null in the flat sense: three of four formats get *worse*, T20I by eight standard
errors, and the fourth moves by a sixth of one. AUC is unchanged everywhere. Nothing
shipped; `DISPLAY_CONTEXT_MONOTONE_KEPT` stays `False`.

**Why it could not have worked, measured two ways.** The recorded diagnosis — the
context columns' `monotonic_cst` is 0, so nothing constrains them — describes the model
correctly and the *probe* not at all. The probe holds team context at the fixture's values,
so those columns do not move under it, and a constraint on a column that does not move
cannot change the count. Two diagnostics beside the arms say so directly:

| format | swap, boosting with **no team context at all** | swap, control (with it) | share of upgrades moving a *constrained* column the wrong way | share moving an *unconstrained* column |
|---|---:|---:|---:|---:|
| T20 | 0.0504 | 0.0478 | 0.000 | **1.000** |
| T20I | 0.1364 | 0.0684 | 0.000 | **1.000** |
| ODI | 0.0352 | 0.0307 | 0.000 | **1.000** |
| TEST | 0.0568 | 0.0616 | 0.000 | **1.000** |

A display model that never sees team context violates as much or more. In feature space,
with no model involved, **no constrained column ever moves against its direction**, and the
one unconstrained column an upgrade moves — on every single upgrade, in every format — is
`t1_pelo_std`, the spread of player Elo across the eleven. Its direction is 0 because it is
genuinely unknown: raising a player above the side's mean widens the spread, raising one
below it narrows the spread, so no sign is defensible. The objective survives the same
column because it is linear and its coefficient there is small; a boosted tree responds in
steps, and some of those steps go down. Constraining team context makes matters worse
presumably because it removes fitting capacity that then routes through the free column —
an explanation the numbers are consistent with and this run did not separately test.

### `B-7-pelo-spread` — the fix the mechanism points at, priced and not taken

Registered as an **informing** gate (`ml/xi/gates.py`), because B-7 scoped a constraint on
team context, not a change to what the display model reads, and dropping a column from
`DISPLAY_FEATURE_COLS` changes a served artifact's feature list. Same folds, same seeds,
same everything else:

| format | swap (control) | swap without `pelo_std` | Δ ± se | display AUC (control) | Δ AUC ± se |
|---|---:|---:|---|---:|---|
| T20 | 0.0478 | **0.0000** | −0.0478 ± 0.0082 | 0.7299 | −0.0004 ± 0.0015 |
| T20I | 0.0684 | **0.0000** | −0.0684 ± 0.0076 | 0.7495 | **+0.0088 ± 0.0062** |
| ODI | 0.0307 | **0.0000** | −0.0307 ± 0.0029 | 0.7083 | −0.0055 ± 0.0039 |
| TEST | 0.0616 | **0.0000** | −0.0616 ± 0.0098 | 0.6431 | −0.0047 ± 0.0052 |

Exactly zero violations in every format and every fold — which is the confirmation that the
mechanism is fully identified, not merely correlated with. **The decision is not this
gate's to make**, and is recorded here so it can be made deliberately:

- **Take it.** The Team Lab's what-if becomes coherent: every upgrade raises the displayed
  probability, in every format. It costs about half a point of AUC in ODI and TEST (−0.0055
  and −0.0047, 1.4 and 0.9 fold-level standard errors — real but not resolved), nothing in
  T20, and *gains* 0.0088 in T20I. It is a change to `DISPLAY_FEATURE_COLS`, so it needs a
  retrain, and H-8 parity and the stored `display_cols` move with it.
- **Leave it.** Nothing selects on this model, the AUC is the number the display model
  exists for, and one swap in twenty going the wrong way is now stated in the harness, the
  glossary, the Evaluation tab and the system map rather than being invisible.

Either way the measurement from part 1 stays.

### Part 3 — the decision: taken, and what it cost

**Taken.** The user's decision, in their words: *"take the pelo_std change, its worth it for
the surface."* The reasoning is a product one, not a statistical one.
[PRODUCT_ROADMAP.md](PRODUCT_ROADMAP.md) § 3 sells interactive what-if — swap a player,
watch the probability move — as the core of the thing, and Phase 1's Team Lab is that
surface. A swap that moves the probability the wrong way one time in twenty undermines the
whole proposition, and half a point of AUC in two formats is a price worth paying for a
surface that is coherent by construction rather than on average.

**What shipped.** `t1_pelo_std` / `t2_pelo_std` are out of `DISPLAY_FEATURE_COLS`, named in
`ml.xi.contract.DISPLAY_EXCLUDED_COLS` with the reason. The **objective is untouched** and
still reads them: it is linear, its coefficient there is small, and H-4 measures 0.0–0.8 %
on it. Because a served artifact's feature list moved, `XiStore.load` now refuses a win
artifact whose `objective_cols` or `display_cols` are not the contract's, naming the run and
saying "Retrain" — the D-6 policy, which the ratings had and the models did not: the serving
path builds its row from the artifact's *own* column list, so an older run would have loaded
without complaint and served exactly the surface this change removes.

**Confirmed against the database, not asserted** (`make evaluate`, 2026-09-06, Postgres
source, 22,818 matches, eleven quarterly folds):

| format | display swap-violation share | folds at zero | display AUC, before → after | Δ |
|---|---:|---:|---:|---:|
| T20 | **0.0000** | 11 / 11 | 0.7299 → 0.7296 | −0.0003 |
| T20I | **0.0000** | 10 / 10 | 0.7533 → 0.7540 | **+0.0007** |
| ODI | **0.0000** | 11 / 11 | 0.7067 → 0.7045 | **−0.0022** |
| TEST | **0.0000** | 11 / 11 | 0.6456 → 0.6397 | **−0.0059** |

Zero violations of 6,107 upgrades in T20, 4,601 in T20I, 5,667 in ODI and 4,835 in TEST —
every fold, not a mean that rounds to zero. **H-8 serving parity is 0.0** (50 matches, 1,100
player rows, 1,100 performance predictions, 50 simulations, `passed`), re-established after
the feature list moved.

**The rest of the report is the control, and it did not move.** Objective AUC 0.6973 /
0.7561 / 0.6725 / 0.6259 and H-4's own swap share 0.0023 / 0.0075 / 0.0000 / 0.0052 are
**bit-identical** to the `main` run part 1 recorded on this database, which is what makes
the display AUC column above attributable to the dropped column and nothing else. E2 still
serves the simulated probability as a probability in every format it runs (Δ Brier +0.0015
T20, +0.0007 T20I, +0.0080 ODI, tolerance 0.01), and E5's verdicts are unchanged.

**The cost, stated plainly.** This was a trade, not a free win. Display AUC fell in three of
four formats, and in TEST by 0.0059 — more than the informing gate's paired archive-frame
reading of −0.0047, and larger than the T20I gain. The gate's figures and this run's differ
in magnitude because the gate paired three seeds per fold on the archive frame while this is
one database run against another (the ground-keying difference X-3 recorded), but the signs
agree in every format and every difference sits inside the gate's own fold-level standard
errors (±0.0015 / ±0.0062 / ±0.0039 / ±0.0052). Nobody reading this later should record it
as an improvement: **the displayed probability discriminates slightly worse, and it was
spent on the Team Lab's coherence, deliberately.**

**Status: B-7 is closed.** The measurement from part 1 stays and now reads zero; the
glossary band, the Evaluation tab's tile and H-4's row in the plan say what the zero cost.

---

## Running the database suites

The 43 `_Integration` tests across `internal/db`, `runplan` and `datasetregistry` are the
only checks that run this project's SQL against Postgres rather than against a mock. Every
one of their fixtures begins with a `TRUNCATE`, so they run against a scratch database:

```sh
createdb -h localhost -U postgres cricket_flow_test   # once
make -C go-app test-db                                 # POSTGRES_DB=cricket_flow_test
```

`dbtest.SkipUnlessScratchDatabase` refuses to run them when `POSTGRES_DB` is unset or names
the working database, because the default connection points at the imported archive and the
first fixture would delete it. `.github/workflows/go-app-ci.yml` runs them on every PR
against its own `postgres:15-alpine` service, which is why B-4's class of defect — a gated
test nothing gates on — cannot recur silently.
| B-20 | SERVE-05 (PR #328) | **The simulator's realised batted share runs 0.14 below the classifier's P(bats) on average, 0.2–0.3 for slots 7–11, and the gap is not SERVE-05.** Measured on the served run `20260920T175255Z-71339c52` over 83 men's fixtures (166 sides, toss known, 4,000 draws): with the depth decision fixed, the depth stage realises each player's own P(bats) (mean gap 0.005), but the whole innings realises mean −0.144 against it — ODI bat-first −0.144, chasing −0.163; T20I −0.125 / −0.163; T20 −0.119 / −0.148 — with 57 % of players more than 0.10 below. By slot in `exp_bat_position` order, bat-first: slot 7 forecast 0.845 realised 0.631, slot 9 0.593 → 0.338, slot 11 0.428 → 0.194. | `ml/xi/simulator.py` `batting_innings`: the depth D is drawn from P(bats), which the classifier fitted on real innings — innings that ended at the overs or the target — so D already prices the innings' length; then `_first_innings_constraints` truncates the batters' independently drawn balls at the deliveries budget in slot order and `_chase` ends the innings at the target, each of which removes batters D had already committed to. The budget alone (bat-first sides) accounts for 0.12–0.14 of it. | `wickets_lost`, the per-player `batted_share`, the scorecard lines and `spread_share` of the lower order all read too shallow an innings, and the classifier's calibrated depth is discarded after being drawn. Totals are bounded by the budget either way. A fix that keeps D and fits the batters' balls inside the budget pro rata (rather than dropping the tail) changes the totals' distribution and so H-22 and E2; it needs the harness, which is why it is recorded rather than folded into SERVE-05. Since PR #328 the gap is visible on `/simulate` as `batted_share` beside `p_bats` (§8.7). | no | open |
| B-21 | GO-07 (PR #331) | **The eleven is picked for the average batting order while the probability beside it is read at the toss.** Since GO-07 a caller who names `team1_bats_first` gets a toss-aware headline and toss-aware per-player forecasts, but the same request's *selection* is unchanged: `/xi/optimize` maximises `objective_probability`, and `XiStore.objective_probability` reads only `side_vectors` aggregates — the objective model has no batting-order column to read — so the identical eleven comes back whichever side bats first. Measured on the served run `20260920T175255Z-71339c52` over 25 recent men's TEST fixtures: `objective_probability` is bit-identical across `team1_bats_first` true / false / absent, while the displayed headline moves 0.042 on average (max 0.136) and the two oriented readings sit 0.083 apart on average (max 0.273). | `ml-service/ml/xi/store.py` `objective_probability` and `xi_feature_vector`; `ml-service/app/xi_service.py` `optimize` and `_constraint_check`. The constraint checks are toss-invariant by construction (they count team size, bowlers, keeper and must-include ids) and are not part of this. | A user who sets the toss and then reads "this is the best eleven" is reading a claim about the marginalised fixture, not about the one they described — the §8.7 shape, one layer up from the number. Closing it is not plumbing: the objective would need a batting-order feature, which means a refit and a re-gate, and it would decouple the optimiser's target from `objective_auc` and H-17 — the manifest key EVAL-05 (#319) had just finished making mean one thing. Whether a toss-dependent selection is even wanted is a product question, not a defect. GO-07 names the carve-out on the wire (`toss.note`) rather than hiding it. | no | open |
| B-22 | IMPORT-08 (PR #334) | **The rating pass's two sources now partition venues differently.** `CricsheetJsonSource` keys a ground by the raw spelling the file used (`info.get("venue") or ""`); `PostgresSource` keys it by `venue_id`, which since IMPORT-08 is the folded identity. So a `--cricsheet-dir` pass sees `M Chinnaswamy Stadium` and `M.Chinnaswamy Stadium` as two grounds while a `--postgres` pass sees one, and likewise for the other three pairs -- 137 of 22,905 matches. The two key spaces were never comparable (ids against names), so this is a difference of *partition*, not of key. | `ml-service/ml/xi/sources.py:444` (JSON) against `:806` (Postgres); the fold is `go-app/internal/venues.NormalizeName` / `ml/weather/venues.py` `venue_key` | `make retrain` and `make evaluate` both default to `--postgres`, so no number the system publishes reads the JSON partition today, and `ml.xi.parity` compares counts and player keys, not venue keys, so it stays green. It matters the moment someone retrains or evaluates from the archive directory to reproduce a run: the venue familiarity and scoring baselines would differ from the published run for four grounds with no count able to say so. The one-line fix is to apply the same fold at `:444`; it is left alone because the venue key is DATA-02's subject and changing it is a measurement change that wants the harness. | no | open |
