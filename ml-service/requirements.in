# Direct dependencies only. Pinned output: pip-compile requirements.in -> requirements.txt
# NOTE: compile-requirements uses Python 3.9 (local venv) but Docker uses Python 3.12. Use
# make compile-requirements-docker to regenerate requirements.txt for the Docker/Python 3.12 env.
fastapi
uvicorn
pydantic
numpy
pandas
scikit-learn==1.5.2
joblib
optuna
structlog
httpx
psycopg2-binary
python-dotenv

# Auto-tune enhancements (optional: auto_tune works without these via fallback)
pycaret
autogluon

# Model interpretability (SHAP for MLP and tree-based auto-tuned models)
shap
