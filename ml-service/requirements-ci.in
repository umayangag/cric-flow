# Minimal dependencies for CI: lint (ruff) + tests (pytest). No pycaret/autogluon.
# Use: pip-compile -U --strip-extras requirements-ci.in -o requirements-ci.txt
# CI runs: fmt-check + coverage. Training/auto-tune deps (pycaret, autogluon, torch, etc.) are optional.
# Pin numpy/pandas to match full requirements.txt to avoid test drift.
fastapi
uvicorn
pydantic
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.5.2
scipy==1.11.4
joblib
optuna
structlog
httpx
psycopg2-binary
python-dotenv
psutil
